{
  "name": "ubus",
  "version": "1.0.1",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "engines": {
    "node": ">=6.9.5"
  },
  "scripts": {
    "clean": "rimraf lib esm",
    "build": "npm run build:commonjs && npm run build:esm",
    "build:commonjs": "tsc --project .",
    "build:esm": "tsc --project . --outdir esm --module esnext",
    "test": "jest -c jest.unit.js",
    "test:integration": "jest -c jest.integration.js",
    "lint": "tslint --project .",
    "prerelease": "git checkout master && git pull --ff-only origin master && npm install && npm run clean && npm run lint && npm run test && npm run build",
    "release": "standard-version",
    "postrelease": "git push --follow-tags origin master && npm publish"
  },
  "repository": {
    "type": "git",
    "url": "mabels/ubus"
  },
  "license": "AFL-2.0",
  "bugs": {
    "url": "https://github.com/mabels/ubus/issues"
  },
  "homepage": "https://github.com/mabels/ubus#readme",
  "devDependencies": {
    "@types/jest": "24.0.9",
    "@types/rimraf": "2.0.2",
    "@types/uuid": "3.4.4",
    "rimraf": "2.6.3",
    "jest": "24.1.0",
    "jest-cli": "24.1.0",
    "tslint": "5.13.1",
    "prettier": "1.16.4",
    "ts-jest": "24.0.0",
    "ts-node": "8.0.2",
    "depcheck": "0.7.2",
    "typescript": "3.3.3333"
  },
  "dependencies": {
    "standard-version": "^5.0.1",
    "uuid": "3.3.2"
  },
  "publishConfig": {
    "tag": "next"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.ts": [
      "prettier --write",
      "git add"
    ]
  },
  "prettier": {
    "singleQuote": true
  },
  "renovate": {
    "extends": [
      "config:base"
    ],
    "semanticPrefix": "chore:",
    "devDependencies": {
      "automerge": true
    },
    "packageRules": [
      {
        "packagePatterns": [
          "@types/"
        ],
        "automerge": true
      }
    ]
  },
  "resolutions": {}
}
